import React from 'react'
import { HomeState } from '@/store/homeState'

interface Props {
	homeState: HomeState
}

const DataViews: React.FC<Props> = (props) => {
	return <div>数据大屏</div>
}

export default DataViews
